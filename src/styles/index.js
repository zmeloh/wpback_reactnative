// styles.js
import { DefaultTheme } from 'react-native-paper';

export const blueTheme = {
  ...DefaultTheme,
  colors: {
    ...DefaultTheme.colors,
    primary: '#2196F3', // Couleur principale (bleu)
    accent: '#FF4081', // Couleur accentuée (rose)
  },
};
